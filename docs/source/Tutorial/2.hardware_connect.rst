.. _hardware_assembly:

2.Hardware Connection
=========================

This chapter walks through the hardware assembly for the LAFVIN AIoT Starter Kit, including the ESP32-S3 main controller and the included modules.

.. warning::
   Before starting assembly, disconnect all power sources. Do not wire the board while it is powered on.

.. note::
   Use the included external power adapter only after the controller board and modules are connected. For first-time flashing, a USB Type-C data cable connected to your computer is usually enough.

.. warning::
   Do not connect high-voltage external loads to the relay module until the board, firmware, and voice control path have already been tested successfully.

GPIO Pin Assignment
----------------------

The following table shows the main control pins used by each module:

.. list-table:: GPIO Pin Assignment Table
   :widths: 25 25 25
   :header-rows: 1

   * - Module Name
     - Function
     - GPIO Pin
   * - RGB LED
     - Red, green, and blue channels
     - GPIO4 GPIO5 GPIO6
   * - DHT11 Sensor
     - Data signal
     - GPIO11
   * - SG90 Servo
     - PWM control
     - GPIO16
   * - WS2812 LED Strip
     - Data signal
     - GPIO7
   * - Fan Control
     - Switch control
     - GPIO10
   * - 2-Channel Relay Module
     - Control signals 1 and 2
     - GPIO8 GPIO9
   * - Rain Sensor
     - Digital signal
     - GPIO15
   * - Soil Sensor
     - Digital signal
     - GPIO17

Assembly Steps
-----------------

**Step 1: Install the main controller**

1. Insert the ESP32-S3 development board, audio board, and screen into the matching headers on the AI Chatbot IoT Shield.
2. Make sure the pins are aligned correctly and are not bent.
3. Insert the AI Chatbot IoT Shield into the breadboard.

.. video:: img/connect/la066_assemble.mp4
   :width: 100%

**Step 2: Connect power**

1. After the controller and modules are installed, connect the included external power adapter to the AI Chatbot IoT Shield.

**Step 3: Connect the sensor modules**

1. Connect the DHT11 sensor according to the pin assignment table.
2. Connect the rain sensor and soil sensor.
3. Check that each connection is secure before powering the board.

.. image:: img/connect/dht11.jpg

.. image:: img/connect/rain_sensor.jpg

.. image:: img/connect/soil_sensor.jpg

**Step 4: Connect the actuator modules**

1. Connect the RGB LED module.
2. Install the servo and WS2812 LED strip.
3. Connect the fan and relay modules.

.. image:: img/connect/rgb_led.jpg

.. image:: img/connect/fan.jpg

.. image:: img/connect/led_strip.jpg

.. image:: img/connect/relay.jpg

.. image:: img/connect/servo.jpg

Connection Verification
--------------------------

**Basic Checklist**

* ESP32-S3 development board installed correctly
* All Dupont wires connected securely
* Signal wires connected according to the wiring table
* USB data cable connected to the computer and ready for firmware upload

**Function Testing**

1. **Power-on Test**: After connecting USB power, the development board indicator light should turn on.
2. **Voice Test**: Say ``Hi, ESP`` to wake the device and listen for a response.
3. **LED Test**: Say ``Hi, ESP, turn on LED`` to test the lighting output.
4. **Sensor Test**: Say ``Hi, ESP, check temperature`` to confirm that the sensor data path is working.

.. tip::
   Test one module at a time. It is much easier to isolate a wiring issue before every module is connected.

Next, continue with software setup and firmware flashing.


.. 有个问题,这个硬件链接章节前还没帮烧录,不确定是否要调整一下顺序,就是讲这些测试的转移到烧录之后进行，或者在这里先讲一下基本的连接和上电测试，烧录之后再进行功能测试。